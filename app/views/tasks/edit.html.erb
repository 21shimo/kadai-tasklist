<h1>id:<%= @task.id %> のタスク詳細ページ</h1>

<%= form_for(@task) do |t| %>
	<%= t.label :content, 'タスク' %>
	<%= t.text_field :content %>
	
	<%= t.submit '登録' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>